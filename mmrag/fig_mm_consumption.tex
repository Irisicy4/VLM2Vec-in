% Figure block for the DATA-SCALING analysis (fig_mm_consumption.pdf) — the consumption axis.
% Distinct from fig_mm_scaling, which is now only the fixed-compute CONTROL (pool size).
%
% REPRO(fig:mm_consumption):
%   MMRAG_DATA=<data> python3 mmrag/plot_scaling.py --consumption-out mmrag/fig_mm_consumption
% x = examples DRAWN = batch_size (4) x steps. NOT exactly unique: train_rl.py:238 does
% rng.sample(rows, batch_size) fresh each step -- no replacement within a step, WITH
% replacement across steps. Expected collisions (birthday, exact): 0.50% at 2k draws over
% the 200k pool, 1.48% at 6k, 2.94% at 12k; 2.18% for the 45k-pool headline cell at 2k.
% Never label this axis 'unique examples'. Checkpoint evals named <parent>-ck<STEP>
% inherit the parent's config and contribute STEP x batch_size. Series are pinned to the pool
% sizes in CONSUMPTION_AVAIL so each CURVE stays within one query distribution (both v1 and v3:
% the 200k pool). The v3 headline cell (45,248-row pool_train) is a legitimate 2k-draw result
% from a DIFFERENT distribution, so it is drawn via CONSUMPTION_OFFCURVE as a hollow unconnected
% marker, never joined into the curve. max_steps is FREE on this axis (it IS the axis) and pinned
% on the pool-size axis; the two collectors deliberately differ.
% The script refuses to emit until a series has >=3 points, so a 2-point "curve" cannot ship.
% Dashed overlay = best-so-far envelope, so a run past its peak cannot read as data-limited.
%
% Values at the commit that produced the current PDF (v1 fresh, 200k pool):
%   2k scale-rows200k 0.3480 | 6k scale-rows200k-s1500(+ -s1) 0.3457 (2 seeds) |
%   12k scale-rows200k-s3000 0.3307;  v3 off-curve: 2k v3-pure x3 0.3471 (45k pool)
% IN FLIGHT: v1 scale200k-ck{250,500,1000,1500,2000,2500} (the dense curve); v3 v3-rows200k
%   (2k big-pool, job 26aba17f -- the point that puts v3 ON the curve), v3-rows200k-s1500
%   (6k, job 749f47f6), v3-rows200k-s3000 (12k, job e89a2ad8).
% Regenerate when they land -- the figure picks them up with no edit to the script.
\begin{figure}[t]
\centering
\includegraphics[width=0.66\textwidth]{fig_mm_consumption.pdf}
\caption{\textbf{Data scaling on the axis that matters: examples actually drawn.} Downstream
VQA accuracy against the number of (image, question, answer) triples the run has drawn
($4 \times$ steps), holding the model (\textsc{GME-Qwen2-VL-2B}) and the recipe fixed and drawing
from the $200$K pool. Draws are near-distinct: each batch is sampled independently, so an example
can recur across steps, but the birthday bound over the $200$K pool caps expected collisions at
$2.9\%$ at $12$K draws and $0.5\%$ at $2$K. Compute follows data by construction,
so this is the curve that answers ``does collecting more cheap VQA supervision buy accuracy?''
--- unlike the fixed-compute control in Figure~\ref{fig:mm_scaling}, where enlarging the pool
changes nothing because the consumed count does not move. Dashed overlay is the best-so-far
envelope, drawn so that a run past its peak cannot be misread as data-limited. Hollow markers are
results at the same budget from a different query distribution (the v3 headline cell on the
$45$K pool), shown unconnected because they are not points of the curve. \textbf{The dense
intermediate-checkpoint cells and every v3 curve point are still in flight}; the joined points
shown are final-step v1 runs, and the paragraph text states in advance how either shape will be
read.}
\label{fig:mm_consumption}
\end{figure}
